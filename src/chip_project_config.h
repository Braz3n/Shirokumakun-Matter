/*
 * CHIP project configuration overrides for Matter AC Controller.
 */

#pragma once

/* One dynamic endpoint slot for the PDM alarm (Contact Sensor EP4). */
#define CHIP_DEVICE_CONFIG_DYNAMIC_ENDPOINT_COUNT 1
